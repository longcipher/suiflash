[package]
name = "suiflash_router"
version = "0.1.0"
edition = "2024"

# Dependencies are automatically managed by Sui framework
# Removed explicit Sui dependency to resolve warning

[addresses]
suiflash =  "0x0" # to be replaced at publish time (renamed from `router`)
